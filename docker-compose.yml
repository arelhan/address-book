services:
  backend:
    build:
      context: ./backend
    expose:
      - "4000"
    environment:
      - DATABASE_URL=file:/app/prisma/dev.db
      - PORT=4000
      - CORS_ORIGIN=*
    volumes:
      - backend-db:/app/prisma
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4000/api/contacts/count"]
      interval: 30s
      timeout: 10s
      retries: 3

  frontend:
    build:
      context: ./frontend
    environment:
      - SERVICE_FQDN_FRONTEND_3000
      - BACKEND_URL=http://backend:4000
    expose:
      - "3000"
    depends_on:
      - backend

volumes:
  backend-db:
